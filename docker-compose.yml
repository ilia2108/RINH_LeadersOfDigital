version: "3.8"
services:
  #Сервис серверной логики
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
  
  #Сервис машинного обучения
  ml:
    build:
      context: ./ML
      dockerfile: Dockerfile
    ports:
      - "5001:5000"